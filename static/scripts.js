document.addEventListener('DOMContentLoaded', function() {
    // Example of removing event listener for the removed button
    // const extractDataBtn = document.getElementById('extract-data-btn');
    // if (extractDataBtn) {
    //     extractDataBtn.addEventListener('click', function() {
    //         // Logic for extracting data
    //     });
    // }

    const otherActionBtn = document.getElementById('other-action-btn');
    if (otherActionBtn) {
        otherActionBtn.addEventListener('click', function() {
            // Logic for other action
        });
    }
});